---
import background from '../assets/background.svg';

const categories = [
  {
    label: 'Servidor',
    items: [
      { name: 'spaCy',   role: 'Detección de LN', color: '#8e78f1' },
      { name: 'Simhash',  role: 'Detección de duplicados',  color: '#8e78f1' },
	  { name: 'ChromaDB',  role: 'Busqueda similitud vectorial',  color: '#8e78f1' },
	  { name: 'Cosine Similarity',  role: 'Detección de similitudes',  color: '#8e78f1' },

    ]
  },
  {
    label: 'Modelo',
    items: [
      { name: 'AST',        role: 'Analisis Estructural', color: '#e0f2fe' },
    ]
  },
  {
    label: 'Herramientas',
    items: [
      { name: 'WCopyfind',         role: 'Detección de coincidencias de frases',  color: '#fff3e0'},
	  { name: 'PyPDF',         role: 'Extracción de texto de PDFs',  color: '#dcd0be'}
    ]
  },
];
---

<div id="container">
  <img id="background" src={background.src} alt="" fetchpriority="high" />
  <main>
    <section id="hero">
      <h1>Anyelolol Stack</h1>
	  <h2>PlagDec</h2>
	<p class="description">
		PlagDec es un módulo basado en inteligencia artificial diseñado para la detección de plagio en documentos digitales. 
		El sistema combina técnicas de procesamiento de lenguaje natural, análisis estructural y comparación semántica 
		para identificar similitudes, duplicados y posibles coincidencias entre textos. 

		Integra herramientas como análisis mediante AST, búsqueda vectorial y algoritmos de similitud, permitiendo 
		evaluar contenido de forma más precisa que los métodos tradicionales basados únicamente en coincidencias exactas.
	</p>

      <div id="categories">
        {categories.map(cat => (
          <div class="category">
            <span class="cat-label">{cat.label}</span>
            <div class="grid">
              {cat.items.map(item => (
                <div class="card">
                  <div class="icon" style={`background:${item.color}`}>{item.icon}</div>
                  <span class="name">{item.name}</span>
                  <span class="role">{item.role}</span>
                </div>
              ))}
            </div>
          </div>
        ))}
      </div>
    </section>
  </main>

</div>

<style>
  #background {
    position: fixed; top: 0; left: 0;
    width: 100%; height: 100%;
    z-index: -1; filter: blur(100px);
  }
  #container {
    font-family: Inter, Roboto, 'Helvetica Neue', Arial, sans-serif;
    min-height: 100vh;
  }
  main {
    display: flex;
    justify-content: center;
    padding: 2rem 1rem 6rem;
  }
  #hero {
    display: flex; flex-direction: column;
    align-items: flex-start; gap: 12px;
    max-width: 700px; width: 100%;
  }
  #badge {
    background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
    color: white; font-size: 0.7rem; font-weight: 600;
    padding: 3px 12px; border-radius: 20px;
    text-transform: uppercase; letter-spacing: 0.05em;
  }
  h1 { font-size: 1.8rem; margin: 0; color: #111827; }
  #hero > p { font-size: 1rem; color: #6b7280; margin: 0; }

  #categories {
    display: flex; flex-direction: column;
    gap: 20px; width: 100%; margin-top: 8px;
  }
  .category { display: flex; flex-direction: column; gap: 8px; }
  .cat-label {
    font-size: 0.72rem; font-weight: 600; color: #6b7280;
    text-transform: uppercase; letter-spacing: 0.08em;
    padding-left: 2px;
  }
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(130px, 1fr));
    gap: 10px;
  }
  .card {
    background: rgba(255,255,255,0.85);
    border: 1px solid rgba(255,255,255,0.9);
    border-radius: 14px; padding: 12px 10px;
    display: flex; flex-direction: column;
    align-items: center; gap: 6px;
    backdrop-filter: blur(10px);
    transition: transform 0.2s;
  }
  .card:hover { transform: translateY(-3px); }
  .icon {
    width: 36px; height: 36px; border-radius: 9px;
    display: flex; align-items: center;
    justify-content: center; font-size: 18px;
  }
  .name { font-size: 0.8rem; font-weight: 600; color: #111827; text-align: center; }
  .role { font-size: 0.7rem; color: #6b7280; text-align: center; }

  .box {
    background: rgba(255,255,255,1);
    border-radius: 16px; border: 1px solid white; padding: 16px;
  }
  #news {
    position: fixed; bottom: 16px; right: 16px;
    max-width: 320px; display: flex; gap: 12px; align-items: flex-start;
    backdrop-filter: blur(50px); transition: background 0.2s;
    text-decoration: none;
  }
  #news:hover { background: rgba(255,255,255,0.55); }
  .news-icon { font-size: 24px; }
  h2 { margin: 0 0 0.4em; font-weight: 600; color: #111827; font-size: 0.95rem; }
  #news p { color: #4b5563; font-size: 0.8rem; line-height: 1.4; margin: 0; }

  @media (max-width: 600px) {
    .grid { grid-template-columns: repeat(2, 1fr); }
    #news { left: 16px; right: 16px; max-width: 100%; position: relative; margin: 1rem; }
  }
</style>